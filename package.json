{
  "name": "cogether",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "cd client && npm install && npm run build",
    "start": "node server/server.js"
  }
}
